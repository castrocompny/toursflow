// Stub usado só pelo Vitest (ver vitest.config.ts). O pacote real
// `server-only` lança erro fora do bundler do Next.js por design — isso é
// o comportamento correto em produção/build, não algo a "corrigir" aqui.
// Este arquivo existe só para os testes rodarem fora do webpack do Next.
export {};
